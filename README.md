This is an example of a `Kafka` consumer listening to K8S resources and storing them in a `PostgreSQL` database.
Used `Jacoco` for code coverage. Omitted `KubernetesController` since it's a demo class.
